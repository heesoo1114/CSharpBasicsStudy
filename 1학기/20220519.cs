using System;

namespace _20220519
{
    internal class Program
    {
        /*
        class Archer
        {
           
            public string name;
            public int arrow;
            public int bow;
            public float expreince;
            static int id;

            public Archer(string name, int arrow, int bow, float expreince)
            {

                if (name == null)
                {
                    Console.WriteLine("잘못된 값입니다. 하나의 문자 이상으로 초기화해야 합니다");
                }
                else
                {
                    this.name = name;
                }
                if(arrow > 0 || bow > 0 || expreince > 0)
                {
                    this.arrow = arrow;
                    this.bow = bow;
                    this.expreince = expreince;
                }
                else
                {
                    Console.WriteLine("잘못된 값입니다. 0 이상의 값으로 초기화 해야 합니다");
                    
                }
                
                


            }
        }

        static void Main(string[] args)
        {
            Archer archerA = new Archer("윤희수", 10, 10, 5.2f);

            Console.WriteLine(archerA.arrow + " " + archerA.expreince);
        }

    }*/
        /*
        class Marathon
        {
            public const double distance = 42.195;

        }
        static void Main(string[] args)
        {
            Console.WriteLine(Marathon.distance);
        }
        */



    }
